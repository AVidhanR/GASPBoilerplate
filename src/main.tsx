import { createRoot } from "react-dom/client";
import CommonGSAPImplementation from "./gsap-things/common-gsap-implem";

createRoot(document.getElementById("root")!).render(
  <>
    <CommonGSAPImplementation />
  </>
);
